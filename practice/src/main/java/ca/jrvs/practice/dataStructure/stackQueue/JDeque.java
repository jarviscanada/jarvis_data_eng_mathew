package ca.jrvs.practice.dataStructure.stackQueue;

public interface JDeque<T> extends JStack<T>, JQueue<T> {
  // This interface is a logical combination of JStack and JQueue with no added functionality
}
